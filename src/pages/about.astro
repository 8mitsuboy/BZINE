---
import BaseLayout from "../layouts/BaseLayout.astro";

const pageTitle = "私について";

const skills = ["TypeScript", "React", "Next.js", "Astro", "Flutter"];
---

<BaseLayout pageTitle={pageTitle}>
  <h2 class="font-heading font-bold text-xl mb-4 text-bzine-light-text dark:text-bzine-text">
    It's a piece of cake
  </h2>

  <p class="text-bzine-light-text-muted dark:text-bzine-text-muted mb-6">
    フロントエンドに生息。かっこいいもの全般が好き。
  </p>

  <h3 class="font-heading font-bold text-lg mb-3 text-bzine-light-text dark:text-bzine-text">Skills</h3>
  <div class="flex flex-wrap gap-2">
    {skills.map((skill) => (
      <span class="px-3 py-1.5 rounded-full text-sm font-medium bg-bzine-accent/15 text-bzine-accent">
        {skill}
      </span>
    ))}
  </div>
</BaseLayout>
